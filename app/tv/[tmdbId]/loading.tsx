import { DetailSkeleton } from "@/components/ui/LoadingSkeleton";

/** Series detail skeleton (header + poster), matching the movie route. */
export default function TVDetailLoading() {
  return <DetailSkeleton />;
}
