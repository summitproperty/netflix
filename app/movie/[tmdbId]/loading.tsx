import { DetailSkeleton } from "@/components/ui/LoadingSkeleton";

/** Detail-page skeleton so the backdrop/poster layout does not jump. */
export default function MovieLoading() {
  return <DetailSkeleton />;
}
